# Exercise: Conditionals

For this assignment, your task is to implement a function that validates
a given password. To be valid, passwords must have at least 8
characters, one of which must be a question mark or an exclamation
point. If a criterion is not met, your function returns a string
describing the error. Otherwise, it returns an empty string.
